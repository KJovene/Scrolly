{% extends 'base.html.twig' %}

{% block body %}
    <h1>Upload Post</h1>

    {{ form_start(form) }}
        {{ form_row(form.image) }}
        {{ form_row(form.title) }}
        {{ form_row(form.description) }}
        <button class="btn btn-primary">Upload</button>
    {{ form_end(form) }}

{% endblock %}